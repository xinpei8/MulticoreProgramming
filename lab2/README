README

Write command "make" in terminal under the file directory /lab2 and run the test with arguments of N (number of threads) and P (port number):
> make
> ./test -n <N> <P> 
> ./test -n 50 2020

When testing with client tools, assign localhost for testing.
> http://localhost:2020